@model Projeto_Controlador_Financeiro_Pessoal.Models.Banco

<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastrar Novo Banco</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>

<body>
    <div class="container mt-5">
        <h1 class="mb-4 text-center">Cadastrar Nova Pessoa</h1>

        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10">
                <form asp-action="Create" class="bg-light p-4 rounded shadow-sm">
                    <div class="form-group mb-3">
                        <label asp-for="NomeBanco" class="form-label"><strong>Nome do Banco</strong></label>
                        <span asp-validation-for="NomeBanco" class="text-danger"></span>
                        <input asp-for="NomeBanco" class="form-control" placeholder="Digite o Nome" />
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Cadastrar" class="btn btn-primary w-100" />
                    </div>
                </form>
                <a asp-action="Index" class="btn btn-success w-100 mt-3">Voltar</a>
            </div>
        </div>
    </div>

</body>

</html>
<br/>
@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger">
        @Html.ValidationSummary(false, "Corrija os seguintes erros: ")
    </div>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}